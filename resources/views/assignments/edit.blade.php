@extends('layouts.app')@section('title',__('Assign Supervisor'))@section('content')<div class="pagehead"><div><h1>{{ __('Assign supervisor') }}</h1><p>{{ __('Connect an active teacher with an active department supervisor.') }}</p></div></div><form class="panel formgrid" method="POST" action="{{route('admin.assignments.update')}}">@csrf<label>{{ __('Teacher') }}<select name="teacher_id" required><option value="">{{ __('Choose teacher') }}</option>@foreach($teachers as $t)<option value="{{$t->id}}" @selected(old('teacher_id') == $t->id)>{{$t->name}} {{$t->supervisor?'· '.__('Current:').' '.$t->supervisor->name:''}}</option>@endforeach</select></label><label>{{ __('Supervisor') }}<select name="supervisor_id" required><option value="">{{ __('Choose supervisor') }}</option>@foreach($supervisors as $s)<option value="{{$s->id}}" @selected(old('supervisor_id') == $s->id)>{{$s->name}} · {{$s->subject}}</option>@endforeach</select></label><div class="formactions"><button class="primary">{{ __('Assign supervisor') }}</button></div></form>@endsection
