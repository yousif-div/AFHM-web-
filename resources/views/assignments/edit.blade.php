@extends('layouts.app')@section('title','Assign Supervisor')@section('content')<div class="pagehead"><div><h1>Assign supervisor</h1><p>Connect an active teacher with an active department supervisor.</p></div></div><form class="panel formgrid" method="POST" action="{{route('admin.assignments.update')}}">@csrf<label>Teacher<select name="teacher_id" required><option value="">Choose teacher</option>@foreach($teachers as $t)<option value="{{$t->id}}">{{$t->name}} {{$t->supervisor?'· Current: '.$t->supervisor->name:''}}</option>@endforeach</select></label><label>Supervisor<select name="supervisor_id" required><option value="">Choose supervisor</option>@foreach($supervisors as $s)<option value="{{$s->id}}">{{$s->name}} · {{$s->subject}}</option>@endforeach</select></label><div class="formactions"><button class="primary">Assign supervisor</button></div></form>@endsection
