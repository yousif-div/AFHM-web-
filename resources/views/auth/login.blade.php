<!doctype html><html lang="{{ app()->getLocale() }}" dir="{{ app()->getLocale() === 'ar' ? 'rtl' : 'ltr' }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ __('Sign in · AFHM') }}</title>@vite(['resources/css/app.css'])</head><body class="login"><div class="login-card">@include('partials.language-switch')<div class="login-brand">AF</div><h1>{{ __('Welcome to AFHM') }}</h1><p>{{ __('English Department Management System') }}</p>@if($errors->any())<div class="alert error">{{ __($errors->first()) }}</div>@endif<form method="POST" action="{{route('login.store')}}">@csrf<label>{{ __('Email') }}<input type="email" name="email" value="{{old('email')}}" required autofocus autocomplete="email"></label><label>{{ __('Password') }}<input type="password" name="password" required autocomplete="current-password"></label><button class="primary full">{{ __('Sign in') }}</button></form><small>{{ __('Contact the administrator if you need account access.') }}</small></div></body></html>
