@extends('layouts.app')@section('title','Teaching Materials')@section('content')<div class="pagehead"><div><h1>Teaching materials</h1><p>Authorized resources for lessons and professional development.</p></div>@if(auth()->user()->role==='admin')<a class="primary" href="{{route('admin.materials.create')}}">Upload material</a>@endif</div><form class="filters"><input name="search" value="{{request('search')}}" placeholder="Search materials"><input name="subject" value="{{request('subject')}}" placeholder="Subject"><input name="grade" value="{{request('grade')}}" placeholder="Grade"><button>Filter</button></form><div class="cards">@forelse($materials as $m)<a class="card" href="{{route('materials.show',$m)}}"><span class="badge on">{{$m->material_type?:strtoupper(pathinfo($m->file_path,PATHINFO_EXTENSION))}}</span><h3>{{$m->title}}</h3><p>{{$m->subject?:'General'}} {{$m->grade?'· '.$m->grade:''}}</p><small>{{\Illuminate\Support\Str::limit($m->description,100)}}</small></a>@empty<p class="empty panel">No materials available.</p>@endforelse</div>{{$materials->links()}}@endsection
