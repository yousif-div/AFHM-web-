@extends('layouts.app')@section('title',__('Teaching Materials'))@section('content')<div class="pagehead"><div><h1>{{ __('Teaching materials') }}</h1><p>{{ __('Authorized resources for lessons and professional development.') }}</p></div>@if(auth()->user()->role==='admin')<a class="primary" href="{{route('admin.materials.create')}}">{{ __('Upload material') }}</a>@endif</div><form class="filters"><input name="search" value="{{request('search')}}" placeholder="{{ __('Search materials') }}"><input name="subject" value="{{request('subject')}}" placeholder="{{ __('Subject') }}"><input name="grade" value="{{request('grade')}}" placeholder="{{ __('Grade') }}"><button>{{ __('Filter') }}</button></form><div class="cards">@forelse($materials as $m)<a class="card" href="{{route('materials.show',$m)}}"><span class="badge on">{{$m->material_type?:strtoupper(pathinfo($m->file_path,PATHINFO_EXTENSION))}}</span><h3>{{$m->title}}</h3><p>{{$m->subject?:__('General')}} {{$m->grade?'· '.$m->grade:''}}</p><small>{{\Illuminate\Support\Str::limit($m->description,100)}}</small></a>@empty<p class="empty panel">{{ __('No materials available.') }}</p>@endforelse</div>{{$materials->links()}}@endsection
