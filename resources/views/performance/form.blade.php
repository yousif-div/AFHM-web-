@extends('layouts.app')@section('title','Generate Performance Report')@section('content')<div class="pagehead"><div><h1>Generate performance report</h1><p>Select a scope; all values are calculated from current records.</p></div></div><form class="panel formgrid" method="POST" action="{{route('performance.store')}}">@csrf<label class="wide">Report title<input name="title" value="{{old('title')}}" required></label><label>Period<input name="period" value="{{old('period')}}" placeholder="August 2026" required></label><label>Teacher<select name="teacher_id"><option value="">All teachers</option>@foreach($teachers as $t)<option value="{{$t->id}}">{{$t->name}}</option>@endforeach</select></label><label>From<input type="date" name="date_from" value="{{old('date_from')}}"></label><label>To<input type="date" name="date_to" value="{{old('date_to')}}"></label><div class="formactions"><button class="primary">Generate report</button></div></form>@endsection
