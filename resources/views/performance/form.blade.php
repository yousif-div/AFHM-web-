@extends('layouts.app')@section('title',__('Generate Performance Report'))@section('content')<div class="pagehead"><div><h1>{{ __('Generate performance report') }}</h1><p>{{ __('Select a scope; all values are calculated from current records.') }}</p></div></div><form class="panel formgrid" method="POST" action="{{route('performance.store')}}">@csrf<label class="wide">{{ __('Report title') }}<input name="title" value="{{old('title')}}" required></label><label>{{ __('Period') }}<input name="period" value="{{old('period')}}" placeholder="{{ __('August 2026') }}" required></label><label>{{ __('Teacher') }}<select name="teacher_id"><option value="">{{ __('All teachers') }}</option>@foreach($teachers as $t)<option value="{{$t->id}}">{{$t->name}}</option>@endforeach</select></label><label>{{ __('From') }}<input type="date" name="date_from" value="{{old('date_from')}}"></label><label>{{ __('To') }}<input type="date" name="date_to" value="{{old('date_to')}}"></label><div class="formactions"><button class="primary">{{ __('Generate report') }}</button></div></form>@endsection
