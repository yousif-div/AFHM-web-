@extends('layouts.app')@section('title','Teaching Reports')@section('content')<div class="pagehead"><div><h1>Teaching reports</h1><p>Weekly and monthly progress from the department.</p></div>@if(auth()->user()->role==='teacher')<a class="primary" href="{{route('teacher.reports.create')}}">Submit report</a>@endif</div><form class="filters"><select name="report_type"><option value="">All types</option><option @selected(request('report_type')==='weekly')>weekly</option><option @selected(request('report_type')==='monthly')>monthly</option></select><button>Filter</button></form><div class="cards">@forelse($reports as $r)<a class="card" href="{{route('reports.show',$r)}}"><span class="badge on">{{$r->report_type}}</span><h3>{{$r->title}}</h3><p>{{$r->teacher->name}} · {{$r->submitted_at->format('M j, Y')}}</p><small>{{\Illuminate\Support\Str::limit($r->content,120)}}</small></a>@empty<p class="empty panel">No teaching reports available.</p>@endforelse</div>{{$reports->links()}}@endsection
