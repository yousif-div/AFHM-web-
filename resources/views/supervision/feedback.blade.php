@extends('layouts.app')@section('title',__('Provide Feedback'))@section('content')<div class="pagehead"><div><h1>{{ __('Feedback for') }} {{$teacher->name}}</h1><p>{{$teacher->subject}} · {{$teacher->grade}}</p></div></div><form class="panel formgrid" method="POST" action="{{route('supervisor.feedback.store',$teacher)}}">@csrf<label>{{ __('Rating') }}<select name="rating">@foreach(range(5,1) as $n)<option value="{{$n}}">{{$n}} / 5</option>@endforeach</select></label><label class="wide">{{ __('Written feedback') }}<textarea name="content" rows="8" required>{{old('content')}}</textarea></label><div class="formactions"><button class="primary">{{ __('Submit feedback') }}</button></div></form>@endsection
