@extends('layouts.app')@section('title','Provide Feedback')@section('content')<div class="pagehead"><div><h1>Feedback for {{$teacher->name}}</h1><p>{{$teacher->subject}} · {{$teacher->grade}}</p></div></div><form class="panel formgrid" method="POST" action="{{route('supervisor.feedback.store',$teacher)}}">@csrf<label>Rating<select name="rating">@foreach(range(5,1) as $n)<option value="{{$n}}">{{$n}} / 5</option>@endforeach</select></label><label class="wide">Written feedback<textarea name="content" rows="8" required>{{old('content')}}</textarea></label><div class="formactions"><button class="primary">Submit feedback</button></div></form>@endsection
