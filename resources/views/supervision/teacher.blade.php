@extends('layouts.app')@section('title',$teacher->name)@section('content')<div class="pagehead"><div><h1>{{$teacher->name}}</h1><p>{{$teacher->subject}} · {{$teacher->grade}} · {{$teacher->email}}</p></div><div class="actions"><a class="primary" href="{{route('supervisor.evaluation.create',$teacher)}}">Evaluate</a><a href="{{route('supervisor.feedback.create',$teacher)}}">Give feedback</a></div></div><div class="grid2"><section class="panel"><h2>Recent reports</h2>@forelse($teacher->teachingReports->take(8) as $r)<a class="row" href="{{route('reports.show',$r)}}"><b>{{$r->title}}</b><span>{{$r->submitted_at->format('M j')}}</span></a>@empty<p class="empty">No reports.</p>@endforelse</section><section class="panel"><h2>Performance history</h2>@foreach($teacher->evaluationsReceived->take(5) as $e)<div class="row"><b>{{$e->score}} / 5</b><span>{{$e->evaluated_at->format('M j')}} · {{$e->comments}}</span></div>@endforeach @foreach($teacher->feedbackReceived->take(5) as $f)<div class="row"><b>{{$f->rating}} / 5 feedback</b><span>{{$f->content}}</span></div>@endforeach</section></div>@endsection
